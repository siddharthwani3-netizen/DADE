{
  "name": "dade-project-root",
  "private": true,
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "start": "node server.js",
    "dev": "cd backend && nodemon server.js",
    "postinstall": "cd backend && yarn install"
  },
  "engines": {
    "node": ">=18"
  }
}
